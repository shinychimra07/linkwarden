// Generated by Skyramp v1.3.13 on 2026-03-17 00:43:26.877124125 +0000 UTC m=+195.166162511
// Command: skyramp generate contract rest http://localhost:3000/api/v1/links/1/share \
// 		--auth-header Cookie \
// 		--framework playwright \
// 		--language typescript \
// 		--method GET \
// 		--output get_share_status_contract.spec.ts \
// 		--output-dir /home/runner/work/linkwarden/linkwarden/tests \
// 		--overwrite \
// 		--provider \
// 		--runtime local \
// 		--skipProvisionParents

// Import of required libraries
import { test, expect } from '@playwright/test';
import { SkyrampClient, checkStatusCode, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
let URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:3000");

// contract test for /api/v1/links/1/share GET
test('testShareGet', async () => {
    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Cookie"] = process.env.SKYRAMP_TEST_TOKEN;
    }

    // Execute Request
    const shareGetResponse = await client.sendRequest({
        url: URL,
        path: "/api/v1/links/1/share",
        method: "GET",
        headers: headers
    });

    // Generated Assertions
    expect(checkStatusCode(shareGetResponse, "20x"), 'status code').toBeTruthy();
});
